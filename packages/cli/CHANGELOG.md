# @makoojs/cli

## 0.2.0

### Minor Changes

- 10646e4: Improve the `makoo inspect` output by grouping resolved project, source, runtime, monkey, injector, injection, and framework data into clearer sections. The inspect output now avoids duplicated config/runtime fields, separates top-level manifest dependencies from module manifest dependencies, and shows injection defaults and injector overrides more explicitly.
